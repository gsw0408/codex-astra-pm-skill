# Copy to .env for local use. Never commit a real .env file.
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=astra-orchestrator
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# Shared resume only: use a private direct PostgreSQL URL with sslmode=require.
# Set this in local .env and GitHub Codespaces Secrets; never commit its value.
# ASTRA_STATE_DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
